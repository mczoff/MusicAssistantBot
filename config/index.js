const router = require('../routes'),
    markups = require('../markups'),
    scenes = require('../scenes')

const musicAssistantBot = {
    Routes: router,
    Markups: markups,
    Scenes: scenes
}

module.exports = musicAssistantBot;